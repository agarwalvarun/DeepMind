#include<iostream>
using namespace std;
void permutation_of_string(char permuted[],char options[],int i)
{
    if (options[0]==0)
    {

    }
    int
}
int main()
{
    char permuted[100];
    char options[100];
    int index=0;
    cout<< "enter the string";
    cin.getline(options,100);
    permutation_of_string(permuted, options,index);

}
